# Block npm usage - Use Yarn instead
# This ensures that npm commands will show a helpful error message

engine-strict=true
save-exact=false
fund=false
audit=false

# If someone accidentally runs npm, show clear message
# Use: yarn install instead